// JavaScript source code
describe('When cancelling a payment', function() {
    beforeEach(module('angularTerminal.controllers'));
    beforeEach(inject(function($controller) {

    }));

    it('Should navigate back to merchant page', function() {

    });
});